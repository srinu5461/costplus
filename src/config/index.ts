/**
 * STATIC CONFIGURATION FILES
 *
 * These files store static content that loads instantly without API calls.
 * Edit these files to change site content - changes take effect immediately.
 */

export { headerConfig } from './header';
export { heroConfig } from './hero';
